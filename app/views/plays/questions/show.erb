<%= link_to fa("arrow-up"), plays_path( quiz_id: @play.quiz_id ), class: "btn btn-outline-primary float-right" %>

<h2><%= @question %></h2>
<hr>
<%= form_with url: play_question_path( @play, @question ), method: :PATCH, local: true do |form| %>
  <% #= @question.ask_html %>
  <%= render 'questions/rendered/ask', question: @question %>
<% end %>
